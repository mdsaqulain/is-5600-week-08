module.exports = {
  // preset: '@shelf/jest-mongodb',
  testEnvironment: 'node',
  // testMatch: ['**/tests/*.test.js'], // Adjust the pattern to match your test files
  // testPathIgnorePatterns: ['/node_modules/'], // Ensure this is not excluding your test files
};